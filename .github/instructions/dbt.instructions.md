---
description: "dbt/SQL skill trigger"
applyTo: "dbt/**/*.sql, dbt/**/*.yml, **/dbt_project.yml"
---

You MUST invoke the `data-expert` skill before writing or modifying dbt models or SQL. This skill contains Vista's data tooling (Snowflake, dbt Stacks, Airflow) and SQL patterns.
